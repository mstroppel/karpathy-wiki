# Shared ShellCheck configuration for all shell scripts in the repository
# (covered by scripts/lint.sh).
#
# SC2016: init.sh intentionally keeps ${VAR} literal inside single-quoted
# heredoc content that is expanded later by the shell writing the file.
disable=SC2016
