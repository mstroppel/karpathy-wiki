# PEP 517 build backend for the ingest wheel builds. This is the single
# source of truth for the setuptools version: every pyproject.toml
# ``build-system.requires`` pin must match it (validated by
# tests/test_dependencies.py, which derives the expected version here).
# The wheel is hash-pinned and preinstalled so
# the wheel build itself can run with --no-build-isolation and never executes
# index-resolved tooling; this file is the only place the index is consulted.
setuptools==83.0.0 \
    --hash=sha256:025bccbbf0fa05b6192bc64ae1e7b16e001fd6d6d4d5de03c97b1c1ade523bef \
    --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
