# PEP 517 build backend for the ingest wheel builds. The version must match
# the ``build-system.requires`` pin in every pyproject.toml (validated by
# tests/test_dependencies.py). The wheel is hash-pinned and preinstalled so
# the wheel build itself can run with --no-build-isolation and never executes
# index-resolved tooling; this file is the only place the index is consulted.
setuptools==83.0.0 \
    --hash=sha256:025bccbbf0fa05b6192bc64ae1e7b16e001fd6d6d4d5de03c97b1c1ade523bef \
    --hash=sha256:29b23c360f22f414dc7336bb39178cc7bcbf6021ed2733cde173f09dba19abb3
